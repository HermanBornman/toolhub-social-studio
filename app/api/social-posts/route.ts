import { NextResponse } from "next/server";import { publishingSchema } from "@/lib/social-publishing";import { ensureCurrentUser,errorResponse } from "@/lib/server-user";import { createSocialPost } from "@/lib/social-service";import { prisma } from "@/lib/prisma";
export async function GET(){return NextResponse.json(await prisma.socialPost.findMany({include:{advertisement:true,channels:{include:{socialChannel:true}}},orderBy:{createdAt:"desc"}}));}
export async function POST(request:Request){try{const user=await ensureCurrentUser();const body=await request.json();const parsed=publishingSchema.safeParse(body);if(!parsed.success)return NextResponse.json({error:"Publishing validation failed",issues:parsed.error.flatten().fieldErrors},{status:400});return NextResponse.json(await createSocialPost({...parsed.data,simulateInstagramFailure:Boolean(body.simulateInstagramFailure)},user),{status:201});}catch(error){const result=errorResponse(error);const message=error instanceof Error?error.message:result.error;const status=message==="ADVERT_NOT_APPROVED"?409:message==="CHANNEL_UNAVAILABLE"?409:message==="SCHEDULE_IN_PAST"?400:result.status;return NextResponse.json({error:message},{status});}}
