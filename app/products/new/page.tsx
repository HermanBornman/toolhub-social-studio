import { redirect } from "next/navigation"; import { AppShell } from "@/components/AppShell"; import { ProductForm } from "@/components/ProductForm"; import { canManageProducts, getCurrentUser } from "@/lib/user-role";
export default async function NewProductPage({searchParams}:{searchParams:Promise<{returnToAdvert?:string}>}) { if(!canManageProducts(getCurrentUser().role))redirect("/products"); const params=await searchParams; return <AppShell title="Add Product" subtitle="Create a reusable product record and transparent cut-out."><ProductForm returnToAdvert={params.returnToAdvert==="true"}/></AppShell>; }
