"use client";import{useState}from"react";
export function AdvertPlanningControl({id,priority,tags,validFrom,validUntil}:{id:string;priority:string;tags:string;validFrom:string;validUntil:string}){const[p,setP]=useState(priority),[t,setT]=useState(tags),[from,setFrom]=useState(validFrom),[until,setUntil]=useState(validUntil),[message,setMessage]=useState("");async function save(){const r=await fetch(`/api/adverts/${id}/planning`,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({planningPriority:p,planningTags:t,validFrom:from||undefined,validUntil:until||undefined})});setMessage(r.ok?"Planning controls saved":"Unable to save planning controls")}return<section className="panel social-panel advert-planning"><span className="section-kicker">CONTENT PLANNING</span><h2>Priority and campaign window</h2><div className="strategy-grid"><label className="field"><span>Priority</span><select value={p} onChange={e=>setP(e.target.value)}>{["LOW","NORMAL","HIGH","URGENT"].map(x=><option key={x}>{x}</option>)}</select></label><label className="field"><span>Planning tags</span><input value={t} onChange={e=>setT(e.target.value)} placeholder="contractor, DIY, winter"/></label><label className="field"><span>Valid from</span><input type="datetime-local" value={from} onChange={e=>setFrom(e.target.value)}/></label><label className="field"><span>Valid until</span><input type="datetime-local" value={until} onChange={e=>setUntil(e.target.value)}/></label></div><button className="primary-button" onClick={save}>Save Planning Controls</button>{message&&<p className="form-message">{message}</p>}</section>}
