import{NextRequest,NextResponse}from"next/server";
export function middleware(request:NextRequest){const path=request.nextUrl.pathname;const phase2Page=/^\/(approvals|calendar|planner|reports|settings|social-posts)(\/|$)/.test(path)||/^\/adverts\/[^/]+\/publish$/.test(path);if(phase2Page)return NextResponse.redirect(new URL("/",request.url));if(path==="/login"||path.startsWith("/api/auth/")||path.startsWith("/_next/")||path.match(/\.(png|jpg|jpeg|webp|svg|ico)$/))return NextResponse.next();const sessionCookie=process.env.NODE_ENV==="production"?"__Host-toolhub-session":"toolhub-session";if(!request.cookies.get(sessionCookie)){if(path.startsWith("/api/"))return NextResponse.json({error:"Authentication required"},{status:401});return NextResponse.redirect(new URL("/login",request.url));}return NextResponse.next();}
export const config={matcher:["/((?!_next/static|_next/image|favicon.ico).*)"]};
